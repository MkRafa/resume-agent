langgraph>=0.2.60
pydantic>=2.9
litellm>=1.55
python-dotenv>=1.0
pymupdf>=1.24
python-docx>=1.1
phonenumbers>=8.13
jinja2>=3.1
rich>=13.9
pytest>=8.3

# Optional: HTML -> PDF. Needs system libs (brew install pango cairo gdk-pixbuf libffi).
# The renderer degrades to HTML-only if this import fails.
# weasyprint>=63.0
